# Contributing

1. Create a feature branch.
2. Add/update metric definitions in `config/metrics_codebook.yaml`.
3. Include SQL tests for new/changed logic in `sql/tests/`.
4. Run the manifest tool to record template changes.
5. Submit a PR with a summary of codebook & SQL diffs.
